# Server-side URL used by the same-origin Next.js API proxy.
DRI_BACKEND_URL=http://127.0.0.1:8000

# Optional future telemetry socket. Leave blank until the backend exposes it.
NEXT_PUBLIC_DRI_WS_URL=

